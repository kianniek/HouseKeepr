import 'package:flutter/material.dart';

class SwitchHouseholdPage extends StatelessWidget {
  const SwitchHouseholdPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Switch household')),
      body: const Center(child: Text('Switch household UI (placeholder)')),
    );
  }
}
